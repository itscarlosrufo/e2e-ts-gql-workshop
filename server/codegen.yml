schema: src/schema/**/*.ts
overwrite: true
watch: true
require:
  - ts-node/register
generates:
  ./src/types.d.ts:
    plugins:
      - typescript-common
      - typescript-server
      - typescript-resolvers
